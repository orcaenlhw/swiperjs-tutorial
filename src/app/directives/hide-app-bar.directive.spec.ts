import { HideAppBarDirective } from './hide-app-bar.directive';

describe('HideAppBarDirective', () => {
  it('should create an instance', () => {
    const directive = new HideAppBarDirective();
    expect(directive).toBeTruthy();
  });
});
