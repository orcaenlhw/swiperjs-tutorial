import { ChangePdpDirective } from './change-pdp.directive';

describe('ChangePdpDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePdpDirective();
    expect(directive).toBeTruthy();
  });
});
